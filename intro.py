#List is a container to store a set of values of any data type 
friends=["A","B","1","32.4",False,"kosh"]
print(friends[len(friends)-1])

friends[0]="Graphes"
print(friends[0])
# We can change the content of existing list i.e it is mutable but we cannot change a string 

#it can be indexed just like a string 
print(friends[1:4])
